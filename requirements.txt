# Pinned against PyPI as of Aug 2026. Versions below are the latest release
# of each package that still supports Python 3.9 (this repo was built/tested
# against the macOS system Python, 3.9.6). If you have Python 3.10+ available,
# you can bump these to the newest releases instead.
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.4
beautifulsoup4==4.15.0
requests==2.32.5
python-multipart==0.0.20
Pillow==11.3.0
python-dotenv==1.2.1

# Gemini SDK. Verified package name/version against PyPI at build time --
# see backend/config.py for the model IDs (kept as env-overridable config
# rather than hardcoded, since this model family moves fast).
google-genai==1.47.0

# Pinned explicitly: google-auth (a google-genai dependency) requires
# cryptography, and cryptography>=49 only ships arm64 macOS wheels -- on an
# Intel Mac that forces a from-source build (needs Rust/openssl-sys) that
# fails on a stock system Python. 48.0.1 is the newest release with an
# x86_64-compatible ("universal2") macOS wheel for cp39.
cryptography==48.0.1

# Optional: enables HEIC hand-photo uploads (PRD 4.2). If this fails to
# install/build on your platform, jpg/png uploads still work fine without it.
pillow-heif==1.1.1

# Testing
pytest==8.4.2
httpx==0.28.1
